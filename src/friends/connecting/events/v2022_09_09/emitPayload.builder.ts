import { buildSchema } from '@sprucelabs/schema'

const acceptConnectionEmitPayloadBuilder = buildSchema({
    id: 'acceptConnectionEmitPayload',
    fields: {},
})

export default acceptConnectionEmitPayloadBuilder
