import { buildSchema } from '@sprucelabs/schema'

const sendReminderEmitPayloadBuilder = buildSchema({
    id: 'sendReminderEmitPayload',
    fields: {},
})

export default sendReminderEmitPayloadBuilder
